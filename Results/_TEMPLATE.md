# &lt;Use-case title&gt;

<!--
  This is the shape MarkdownReporter emits for every run. One file per use case, named <use-case-id>.md.
  It is overwritten on each run — don't hand-edit the numbers; re-run the use case instead.
  Generate a real one with:  dotnet run --project src/Benchmark.Cli -c Release -- run <id>
-->

`<use-case-id>` · **&lt;Category&gt;** · engine: `<Stopwatch|BenchmarkDotNet>`

> &lt;One- or two-sentence description of what the use case exercises.&gt;

| Field | Value |
|---|---|
| Generated | &lt;UTC timestamp&gt; |
| Machine | &lt;machine name&gt; |
| OS | &lt;OS description&gt; |
| Runtime | &lt;.NET runtime&gt; |
| CPUs | &lt;processor count&gt; |

## Results

<!-- Rows = steps. Columns = variants (first is the baseline); Δ% is each variant's gain vs the baseline,
     sign-flipped for "lower is better" metrics so a positive number always means "improved". -->

| Step | &lt;Variant 0 (baseline)&gt; | &lt;Variant 1&gt; | Δ% |
|---|---:|---:|---:|
| &lt;step name&gt; | &lt;value&gt; &lt;unit&gt; (&lt;ms&gt; ms) | &lt;value&gt; &lt;unit&gt; (&lt;ms&gt; ms) | &lt;+/-N.N%&gt; |

<sub>Generated by Benchmark.Cli · &lt;UTC timestamp&gt;</sub>

<!--
  Everything ABOVE is regenerated on each run. Anything BELOW the notes marker is hand-written and
  preserved across re-runs — put narrative analysis there.
-->

---

<!-- notes:keep-below — content under this line survives regeneration -->

## Notes

_Hand-written analysis — kept across re-runs. Add yours here._
