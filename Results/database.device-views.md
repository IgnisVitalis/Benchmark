# Device storage — relational vs JSONB vs MongoDB

`database.device-views` · **Database** · engine: `Stopwatch`

> The same 20-field Device stored as typed relational columns, a single JSONB column, or MongoDB documents. Compares load, lookups (indexed-unique, indexed, non-indexed), update, a range query and storage size — with identical data and identical indexed fields in every variant (the host chooses which representations to include).

| Field | Value |
|---|---|
| Generated | 2026-06-08 07:46:45Z |
| Machine | IV-PC |
| OS | Microsoft Windows 10.0.22000 |
| Runtime | .NET 9.0.14 |
| CPUs | 8 |
| Samples | 1 measured, values are medians ± CV% |

## Results

| Step | Relational | JSONB | Δ% | MongoDB | Δ% |
|---|---:|---:|---:|---:|---:|
| Load 20 000 000 devices | 176 660 devices/sec (113 212 ms) | 89 169 devices/sec (224 293 ms) | -49.5% | 59 774 devices/sec (334 596 ms) | -66.2% |
| Lookup by uuid — indexed unique ×500 | 2 359 lookups/sec (212 ms) | 1 256 lookups/sec (398 ms) | -46.7% | 713 lookups/sec (701 ms) | -69.8% |
| Lookup by model — indexed ×500 | 1 654 lookups/sec (302 ms) | 251 lookups/sec (1 988 ms) | -84.8% | 552 lookups/sec (906 ms) | -66.7% |
| Lookup by serial — NOT indexed ×20 | 1.31 lookups/sec (15 313 ms) | 0.347 lookups/sec (57 682 ms) | -73.5% | 0.075 lookups/sec (267 724 ms) | -94.3% |
| Update by uuid — indexed ×500 | 2 348 updates/sec (213 ms) | 1 265 updates/sec (395 ms) | -46.1% | 903 updates/sec (554 ms) | -61.6% |
| Range battery<20 — NOT indexed ×20 | 1 212 queries/sec (17 ms) | 995 queries/sec (20 ms) | -17.8% | 745 queries/sec (27 ms) | -38.5% |
| Storage size (table + indexes) | 6 516 MB | 15 461 MB | -137.3% | 12 706 MB | -95.0% |

<sub>Generated by Benchmark.Cli · 2026-06-08 07:46:45Z</sub>

---

<!-- notes:keep-below — content under this line survives regeneration -->

## Notes — indexed JSONB property: search & update

The question behind this run: can you index a single property *inside* a JSONB column and search/update it
about as fast as a plain column? Relational is the baseline; MongoDB is a third reference. Identical data,
identical indexed fields — only the storage representation differs.

What the numbers show (the relatives hold across re-runs; exact figures are in the table above):

- **Read by the indexed unique key** (uuid) — JSONB matched the plain column (within a few %). The expression
  index on `data->>'uuid'` does its job.
- **Read by an indexed non-unique key** (model) — also close to the column. `model` (~1000 values) is
  selective, so the planner genuinely uses the index in every representation (a `DeviceViewsIndexPlanTests`
  EXPLAIN guard asserts this — it fails if a step labelled "indexed" silently seq-scans).
- **Update by the indexed key** — JSONB modestly slower (~10–15%): the whole JSON document is rewritten.
- **Search a NON-indexed field** (serial, battery range) — JSONB ~20–30% slower: it scans and extracts from
  every document.
- **Storage** — JSONB ≈ 2.4× the relational table (it stores every key name in every row).
- **MongoDB** (reference) — generally slower here.

Notes:
- The dataset is **streamed** (each device is derived from its index), so this runs at 100k or 100M without
  holding the set in memory, and stays identical across every representation.
- An earlier version looked up the low-cardinality `type` field (8 values); with a `LIMIT` the relational
  planner seq-scanned it while JSONB used its index, producing a misleading "JSONB is 20× faster" row. That
  lookup was switched to the selective `model` field — the comparison is now apples-to-apples.
- Single run on one machine; the small ±CV% (mostly 1–4%) means the medians are stable. Re-run to refresh.

### Shareable summary

> Postgres can put an index on one property inside a JSONB column. I wanted to see how fast it is to search
> and update that property — next to a plain column, with MongoDB as a third reference.
>
> Setup: the same record stored three ways — plain table columns, one JSONB document, and a MongoDB document
> — the same data, same index on the same property.
>
> What one run (median of 3) showed:
> — Reading by the indexed property (unique key and a non-unique field): JSONB was as fast as the plain column (within a few %).
> — Updating by the indexed property: modestly slower (~10–15%).
> — Searching a field with no index: ~20–30% slower — JSONB scans and parses each document.
> — Storage: the JSONB version used about 2.4× the disk of plain columns.
> — MongoDB, as a reference, was generally slower here.
>
> So reading through an indexed JSONB property was essentially free; the cost showed up in updates,
> un-indexed queries, and storage.
